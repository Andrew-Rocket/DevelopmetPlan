<%=@department.title %>
<%=link_to 'Delete', department_path(@department), method: :delete%>
<%=link_to 'Edit', edit_department_path(@department)%>
<%= image_tag((@department.cover_img), class: '') %>